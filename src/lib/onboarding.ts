// 처음 들어온 사람에게 한 번 보여주는 안내.
//
// 이 앱은 QR을 찍고 들어온다. 포커는 치지만 솔버는 처음인 사람이 대부분이고,
// 그런 사람에게 "EV 손실 0.3bb"는 아무 뜻도 아니다. 뜻을 모르면 화면의 숫자가
// 그냥 점수로 읽히고, 점수로 읽히면 배우는 게 아니라 맞히기가 된다.
//
// 그래서 네 장만 보여준다. 더 길면 넘겨버리고, 넘겨버리면 없는 것과 같다.
// 읽을거리는 배우기 탭에 따로 있다 — 여기서는 화면을 읽는 법만 다룬다.

const SEEN_KEY = "gto.onboarding.v1";

export type GuideCard = {
  title: string;
  /** 한 장에 문단 두 개까지. 그 이상은 읽지 않는다. */
  body: string[];
  /** 등급표를 함께 보여줄 장인가. */
  showGrades?: boolean;
};

export const GUIDE_CARDS: GuideCard[] = [
  {
    title: "한 판을 끝까지 칩니다",
    body: [
      "프리플랍부터 리버까지, 아홉 자리 중 한 자리에 앉아 실제로 한 판을 칩니다. " +
        "자리는 판마다 바뀝니다 — UTG에서 맞는 선택과 버튼에서 맞는 선택은 다르기 때문입니다.",
      "나머지 여덟 자리는 솔버가 계산한 대로 움직입니다. 상대가 이상하게 치는 것 같다면 " +
        "대개는 그쪽이 맞습니다.",
    ],
  },
  {
    title: "정답이 아니라 손해로 봅니다",
    body: [
      "포커에는 하나의 정답이 없습니다. 같은 패로 접어도 되고 올려도 되는 자리가 있습니다. " +
        "그래서 맞았다·틀렸다 대신, 가장 좋은 선택에 비해 얼마나 손해였는지를 bb로 보여줍니다.",
      "0.02bb 손해는 사실상 같은 선택입니다. 1bb 손해는 한 판에서 큰 블라인드 하나를 " +
        "그냥 버린 것입니다.",
    ],
    showGrades: true,
  },
  {
    title: "13×13 격자는 내 레인지입니다",
    body: [
      "판단이 끝나면 격자가 나옵니다. 가로세로는 두 장의 숫자이고, 오른쪽 위 삼각형은 " +
        "같은 무늬(수딧), 왼쪽 아래는 다른 무늬(오프수트)입니다.",
      "칸의 색은 그 패로 무엇을 해야 하는지입니다. 내가 받은 패의 칸에는 테두리가 " +
        "그려집니다. 한 칸이 아니라 전체 모양을 보세요 — 외울 것은 칸이 아니라 경계선입니다.",
    ],
  },
  {
    title: "틀린 자리는 다시 옵니다",
    body: [
      "손해가 컸던 판단은 기록에 쌓이고, 기록 탭의 복습에서 같은 자리를 다시 물어봅니다. " +
        "반복해서 틀린 것이 위로 옵니다.",
      "한 판 치고 끝내지 말고, 열 판쯤 친 뒤에 복습을 열어보세요. 어디가 새는지는 " +
        "한 판으로는 보이지 않습니다.",
    ],
  },
];

/** 이 사람이 안내를 이미 봤는가. 서버에서는 알 수 없으므로 항상 봤다고 답한다. */
export function hasSeenGuide(): boolean {
  if (typeof window === "undefined") return true;
  try {
    return window.localStorage.getItem(SEEN_KEY) === "1";
  } catch {
    // 사파리 비공개 모드 등에서 저장소가 막힌다. 안내를 못 여는 것보다
    // 매번 보여주는 편이 낫지만, 매번 보여주면 성가시다 — 봤다고 친다.
    return true;
  }
}

export function markGuideSeen(): void {
  if (typeof window === "undefined") return;
  try {
    window.localStorage.setItem(SEEN_KEY, "1");
  } catch {
    // 저장이 막혔으면 다음에 또 보게 된다. 그뿐이다.
  }
}
